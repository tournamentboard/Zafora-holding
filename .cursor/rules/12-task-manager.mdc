---
description: Phase tracker — completed and pending frontend/backend work
alwaysApply: true
---

# Task Manager

## Frontend Phases

| Phase | Status | Summary |
|-------|--------|---------|
| F1 — Foundation | ✅ Done | lib/url-helpers, lib/api-helpers, lib/constants, lib/types, lib/enums |
| F2 — Routing Shell | ✅ Done | (public)/ + (admin)/ route groups, layouts, middleware.ts |
| F3 — UI System | ✅ Done | shadcn/Radix, Navbar, Footer, branding constants, sonner provider |
| F4 — API Layer | ✅ Done | apiAxios, apiClient, TanStack Query, module services pattern, Zod validators |
| F5 — Public Pages | ✅ Done | Home, About, Services, Projects, Government, Submit pages |
| F6 — Public Forms | ✅ Done | submitLead Server Action, ExpressInterestModal, lib/actions/public.ts |
| F7 — Auth Flow | ✅ Done | JWT access+refresh tokens, middleware.ts, requireAdmin(), login page |
| F8 — Admin Shell | ✅ Done | AdminSidebar, AdminHeader, route-based admin pages (/admin/leads etc.) |
| F9 — Admin Modules | ✅ Done | All components migrated to modules/admin/*, api-client-react deprecated |
| F10 — Tables/Search | ✅ Done | StatusBadge, TableToolbar, useDebouncedSearch, useTableFilters |
| F11 — File Uploads | ⏳ Pending | S3 presign route, use-s3-upload hook, ImageUploader component |
| F12 — Error/Loading | ⚠️ Partial | Group-level loading.tsx done; per-segment (leads/projects/etc.) missing |
| F13 — SEO | ⏳ Pending | generateMetadata(), sitemap.ts, robots.ts |
| F14 — RBAC UI | ⏳ Future | Role-based sidebar/actions when backend supports roles |

## Backend Phases

| Phase | Status | Summary |
|-------|--------|---------|
| B1 — DB Foundation | ✅ Done | Neon schema, all tables + faqs, Drizzle migrations |
| B2 — Shared Layer | ✅ Done | error-handler, api-helpers, route-paths, CORS, logger |
| B3 — Auth Module | ✅ Done | JWT login/logout/refresh/change-password/setup-status/setup/reset-password, requireAuth, bcrypt |
| B4 — Public APIs | ✅ Done | Public GET: projects, services, testimonials, content, stats, faqs; POST: leads, interests |
| B5 — Admin APIs | ✅ Done | All admin CRUD behind requireAuth, audit logging, pagination |
| B6 — File Storage | ⏳ Pending | AWS S3 presigned URLs, storage module (replace Replit GCS) |
| B7 — Notifications | ⏳ Pending | Resend email — RESEND_API_KEY must be set in Railway env |
| B8 — Error/Logging | ⏳ Pending | Structured pino logger, standard ApiError shape |
| B9 — Security | ⏳ Pending | Rate limiting, Helmet, body size limits, login slow-down |

## Replit Sync Phases

| Phase | Status | Summary |
|-------|--------|---------|
| R1 — FAQs Backend | ✅ Done | faqs schema, module (routes/service/validator), swagger docs |
| R2 — Setting Defaults | ⚠️ Partial | Missing `services_page`, `government_page`, `submit_page`, full `about`, `seo_*.og*` |
| R3 — Frontend Module Setup | ✅ Done | modules/admin/faqs/, new ROUTES.ADMIN.* constants, API.CONTENT.FAQS* |
| R4 — New Admin Components | ✅ Done | FaqManager, AnnouncementManager, MaintenanceManager, LegalPagesEditor, SectionVisibilityManager + 5 route pages |
| R5 — Public Pages & Layout | ✅ Done | /privacy, /terms, /maintenance pages, maintenance middleware, AnnouncementBar component, sticky header removed |
| R6 — Photo Upload | ⏳ Pending | PhotoUploadField + use-image-upload endpoint fix (blocks all admin image uploads) |
| R7 — Auth Enhancements | ✅ Done | setup-status, setup, reset-password endpoints + ADMIN_SETUP_EMAIL env |
| R8 — Seed Updates | ✅ Done | FAQs + new site_settings keys + 6 real projects seeded |
| R8b — Seed Defaults Corrected | ✅ Done | services_page/government_page/submit_page seeds fixed; prod DB migrated + seeded |
| R9 — Verification | ⏳ Pending | End-to-end verify all R1–R8 features in staging |
| R10 — Admin Mobile Nav | ⏳ Pending | Port `Admin.tsx:619–637` horizontal tab bar to `AdminMobileNav.tsx` (mobile regression) |
| R11 — Public Navbar Restore | ⏳ Pending | Restore h-40 + tagline (currently regressed to h-20) |
| R12 — Section Visibility wiring | ⏳ Pending | Create `lib/site-settings.ts` + consume in all public RSC pages |

## Small Remaining Gaps (Not a Full Phase)

| Item | File | Priority |
|------|------|----------|
| Admin mobile tab bar | `modules/admin/shared/components/AdminMobileNav.tsx` (new) | **P0** |
| AdminHeader ROUTE_META | `modules/admin/shared/components/AdminHeader.tsx` (5 entries) | **P0** |
| Public Navbar restore | `components/common/Navbar.tsx` (h-40 + tagline) | P1 |
| PhotoUploadField component | `modules/admin/shared/components/PhotoUploadField.tsx` | P1 |
| use-image-upload endpoint fix | `hooks/use-image-upload.ts` (call `API.STORAGE.PRESIGN`) | P1 |
| Create `lib/site-settings.ts` helper | `lib/site-settings.ts` | P2 |
| Use section_visibility in public pages | All public RSC pages | P2 |
| Complete SETTING_DEFAULTS | `content.service.ts` (4 keys + SEO OG) | ✅ Done |

## Cleanup Required (After F11)

- Delete `zafora-frontend/src/lib/api-client-react/`
- Delete `zafora-frontend/src/lib/api-zod/`
- Delete `zafora-frontend/src/lib/api-spec/`
- Delete `zafora-frontend/src/lib/object-storage-web/`
- Delete `zafora-frontend/src/lib/db/`

## Next Recommended Actions (Updated 2026-05-26 — post Replit re-audit)

**P0 (mobile regression — fastest wins):**
1. Create `AdminMobileNav.tsx` — horizontal scrollable pill nav, port from Replit `Admin.tsx:619–637`
2. Extend `AdminHeader.ROUTE_META` with FAQ / Announcement / Maintenance / Legal / Section Visibility entries

**P1 (image upload pipeline + Navbar polish):**
3. Restore Navbar `h-40` + tagline display
4. Migrate `PhotoUploadField` from `artifacts/zafora/src/components/admin/`
5. Fix `use-image-upload.ts` to call `API.STORAGE.PRESIGN` with new `{ uploadUrl, publicUrl, key }` shape
6. Wire `PhotoUploadField` into BrandingManager, TeamManager, AboutEditor, ServicesManager, ProjectsTable, TestimonialsManager
7. Set `AWS_S3_CUSTOM_DOMAIN` on Vercel + Railway

**P2 (settings + section visibility):**
8. Complete `SETTING_DEFAULTS` (services_page, government_page, submit_page, full about, seo_*.og*)
9. Create `lib/site-settings.ts` → `getSectionVisibility()` → use in public RSC pages
4. B9: Add `helmet` + `express-rate-limit` to `zafora-backend/src/app.ts`
5. F12: Add `loading.tsx` + `error.tsx` to each individual admin route segment
6. F13: `generateMetadata()` on public pages + `sitemap.ts` + `robots.ts`
7. B7: Set `RESEND_API_KEY` in Railway, test `/api/notifications/test`
