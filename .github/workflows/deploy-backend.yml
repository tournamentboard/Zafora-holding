name: Backend — CI & Railway Deploy

on:
  push:
    branches:
      - main
      - development
    paths:
      - "zafora-backend/**"
      - ".github/workflows/deploy-backend.yml"
  pull_request:
    branches:
      - main
      - development
    paths:
      - "zafora-backend/**"

# Cancel any in-progress run for the same branch
concurrency:
  group: backend-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─────────────────────────────────────────────
  # CI — runs on every push & PR
  # ─────────────────────────────────────────────
  ci:
    name: CI — Typecheck & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: zafora-backend/package-lock.json

      - name: Install dependencies
        working-directory: zafora-backend
        run: npm ci

      - name: Typecheck
        working-directory: zafora-backend
        run: npm run typecheck

      - name: Build
        working-directory: zafora-backend
        run: npm run build

  # ─────────────────────────────────────────────
  # Deploy to DEV — only on push to `development`
  # ─────────────────────────────────────────────
  deploy-dev:
    name: Deploy → Railway (Development)
    runs-on: ubuntu-latest
    needs: ci
    if: github.ref == 'refs/heads/development' && github.event_name == 'push'
    environment:
      name: development
      url: ${{ secrets.DEV_BACKEND_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli@latest

      - name: Deploy to Railway (development environment)
        working-directory: zafora-backend
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          railway up \
            --service "${{ secrets.RAILWAY_BACKEND_SERVICE_NAME }}" \
            --environment development \
            --detach

      - name: Post deployment comment
        uses: actions/github-script@v7
        if: success()
        with:
          script: |
            const sha = context.sha.substring(0, 7);
            github.rest.repos.createCommitComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
              body: [
                '✅ **Backend → Railway Development** deployed for `' + sha + '`',
                '',
                '🔗 API URL: ${{ secrets.DEV_BACKEND_URL }}',
                '📋 Swagger: ${{ secrets.DEV_BACKEND_URL }}/api/docs',
              ].join('\n')
            });

  # ─────────────────────────────────────────────
  # Deploy to PROD — only on push to `main`
  # ─────────────────────────────────────────────
  deploy-prod:
    name: Deploy → Railway (Production)
    runs-on: ubuntu-latest
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: ${{ secrets.PROD_BACKEND_URL }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Railway CLI
        run: npm install -g @railway/cli@latest

      - name: Deploy to Railway (production environment)
        working-directory: zafora-backend
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          railway up \
            --service "${{ secrets.RAILWAY_BACKEND_SERVICE_NAME }}" \
            --environment production \
            --detach

      - name: Post deployment comment
        uses: actions/github-script@v7
        if: success()
        with:
          script: |
            const sha = context.sha.substring(0, 7);
            github.rest.repos.createCommitComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              commit_sha: context.sha,
              body: [
                '🚀 **Backend → Railway Production** deployed for `' + sha + '`',
                '',
                '🌐 API URL: ${{ secrets.PROD_BACKEND_URL }}',
                '📋 Swagger: ${{ secrets.PROD_BACKEND_URL }}/api/docs',
              ].join('\n')
            });
