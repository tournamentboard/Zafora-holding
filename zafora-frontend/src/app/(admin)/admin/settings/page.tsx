import SettingsPanel from "@/src/modules/admin/settings/components/SettingsPanel";

export const metadata = { title: "Settings — Zafora Admin" };

export default function SettingsPage() {
  return <SettingsPanel />;
}
