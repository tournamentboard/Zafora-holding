import LeadsTable from "@/src/modules/admin/leads/components/LeadsTable";

export const metadata = { title: "Inquiries — Zafora Admin" };

export default function LeadsPage() {
  return <LeadsTable />;
}
