import ProjectsTable from "@/src/modules/admin/projects/components/ProjectsTable";

export const metadata = { title: "Projects — Zafora Admin" };

export default function ProjectsPage() {
  return <ProjectsTable />;
}
