import SectionVisibilityManager from "@/src/modules/admin/content/components/SectionVisibilityManager";

export const metadata = { title: "Section Visibility — Zafora Admin" };

export default function AdminSectionVisibilityPage() {
  return <SectionVisibilityManager />;
}
